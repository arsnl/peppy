---
ruleName: "react/boolean-prop-naming"
version: "3.1.1"
jsEntry:
  [
    "off",
    {
      "message": "",
      "propTypeNames": ["bool", "mutuallyExclusiveTrueProps"],
      "rule": "^(is|has)[A-Z]([A-Za-z0-9]?)+",
    },
  ]
previousJsEntry:
  [
    "off",
    {
      "message": "",
      "propTypeNames": ["bool", "mutuallyExclusiveTrueProps"],
      "rule": "^(is|has)[A-Z]([A-Za-z0-9]?)+",
    },
  ]
tsEntry:
  [
    "off",
    {
      "message": "",
      "propTypeNames": ["bool", "mutuallyExclusiveTrueProps"],
      "rule": "^(is|has)[A-Z]([A-Za-z0-9]?)+",
    },
  ]
previousTsEntry:
  [
    "off",
    {
      "message": "",
      "propTypeNames": ["bool", "mutuallyExclusiveTrueProps"],
      "rule": "^(is|has)[A-Z]([A-Za-z0-9]?)+",
    },
  ]
updates: ["2.0.0"]
---
