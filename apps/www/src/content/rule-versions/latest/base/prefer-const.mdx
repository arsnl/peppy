---
version: "3.1.1"
ruleKey: "prefer-const"
description: Require `const` declarations for variables that are never reassigned after declared
jsEntry: ["error", { "destructuring": "any", "ignoreReadBeforeAssign": true }]
previousJsEntry: null
tsEntry: ["error", { "destructuring": "any", "ignoreReadBeforeAssign": true }]
previousTsEntry: null
history: [{ "jsState": "new", "tsState": "new", "version": "3.1.1" }]
---

<RuleInfos 
  configKey="base" 
  ruleKey="prefer-const" 
>

```json
["error", { "destructuring": "any", "ignoreReadBeforeAssign": true }]
```

```json
["error", { "destructuring": "any", "ignoreReadBeforeAssign": true }]
```

</RuleInfos>
