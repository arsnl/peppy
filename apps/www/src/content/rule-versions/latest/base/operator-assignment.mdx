---
version: "3.1.1"
ruleKey: "operator-assignment"
description: Require or disallow assignment operator shorthand where possible
jsEntry: ["error", "always"]
previousJsEntry: null
tsEntry: ["error", "always"]
previousTsEntry: null
history: [{ "jsState": "new", "tsState": "new", "version": "3.1.1" }]
---

<RuleInfos 
  configKey="base" 
  ruleKey="operator-assignment" 
>

```json
["error", "always"]
```

```json
["error", "always"]
```

</RuleInfos>
